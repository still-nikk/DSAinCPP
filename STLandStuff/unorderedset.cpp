//Can only store single occurence of the element i.e. unique element only but
//The order of storing the elements is randomized.
//For everything the time complexity is O(1) except lower_bound and upper_bound don't work
//Worst case time complexity is O(n);

#include <bits/stdc++.h>
using namespace std;

int main() 
{

    unordered_set<int> us;
    //everything is same as a set

    return 0; 
}